import * as vscode from 'vscode';

function registerGoToCommand(context: vscode.ExtensionContext) {
    return vscode.commands.registerCommand("bedrockLantern.goToSymbol", async (element: vscode.TreeItem) => {

    })
}